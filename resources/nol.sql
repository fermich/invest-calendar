create table nol_data (
    timestamp timestamp,
    typ varchar(10),
    px varchar(10),
    sz bigint,
    tov decimal(20, 2),
    dt varchar(30),
    tm varchar(30),
    sym varchar(30),
    id varchar(30),
    src varchar(30)
    );
